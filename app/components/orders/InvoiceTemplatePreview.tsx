// This is a temporary file to fix Tailwind CSS build issues
// It will be removed once the build system no longer references it

import React from 'react';

const InvoiceTemplatePreview: React.FC = () => {
  return <div>Temporary file to fix build issues</div>;
};

export default InvoiceTemplatePreview;
